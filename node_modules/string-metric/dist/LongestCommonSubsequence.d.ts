import { StringDistance } from "./interfaces/StringDistance";
export declare class LongestCommonSubsequence implements StringDistance {
    distance(s1: string, s2: string): number;
    length(s1: string, s2: string): number;
}
